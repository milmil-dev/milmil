---
name: milmil
description: Use when the user mentions milmil — controlling their anime media library, fixing matches, finding episodes to watch, or auditing past actions. Run `milmil agents-guide` first to load the full CLI surface.
---

# milmil

When the user asks anything about milmil, run `milmil agents-guide` to load the full usage guide. Then follow its recipes. Always pass `--json` when piping output between commands. Confirm destructive operations (`match auto`, `match undo`, `token revoke`) with the user unless they explicitly ask you to run with `--yes`.
