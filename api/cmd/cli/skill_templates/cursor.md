# milmil CLI

When the user mentions milmil (their self-hosted anime media server),
run `milmil agents-guide` in the terminal to load the full usage guide
before performing any action. Always pass `--json` when piping CLI
output between commands.
