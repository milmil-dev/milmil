<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- The darkest stop of the brand tile gradient. A launcher masks the
         icon to its own shape, so the background has to be a flat colour
         rather than the gradient the square tile uses. -->
    <color name="ic_launcher_background">#0B0B12</color>
</resources>
